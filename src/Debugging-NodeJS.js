// #1 - If your browser supports debugging, you can use console.log() to display JavaScript values in the debugger window:
console.log("Debug message");

// #2 - The debugger keyword stops the execution of JavaScript, and calls (if available) the debugging function
debugger;

// #3 - Execute the program by inspecting
//type `debugger;` in the code and execute the statement below:
// node inspect program.js
//or
// node--inspect - brk program.js

//Open chrome and load this url `chrome://inspect`
